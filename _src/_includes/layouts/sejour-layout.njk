---
layout: base-layout
ogImage: /images/sejour-plein-air.jpg
ogImageAlt: Photo du Séjour plein air de la Techniques en loisir du Cégep de Saint-Laurent
---
<main>
{% contentHeader
heading = title,
description = "Accédez à l'information spécifique pour l'année de déploiement"
%}
<nav>
    <ul class="tags-container">
        <li class="tags sejour-tags"><a href="{{ '/sejour-plein-air/' | url }}" class="sejour-nav {{ 'active' if page.url === '/sejour-plein-air/' | url }}">Information générale</a></li>
        {% for sejour in collections.sejours | reverse %}
        <li class="tags sejour-tags"><a href="{{ sejour.url | url }}" class="sejour-nav {{ 'active' if page.url === sejour.url | url }}">{{ sejour.data.year }}</a></li>
        {% endfor %}
    </ul>
</nav>
<div class="content">
    {{ content | safe }}
</div>
</main>