---
layout: base-layout.njk
---
<main class="post-layout dark-pink-theme">
    <article>
        <h1>{{ title }}</h1>
        <p><small>
            Écrit par : {{ author[writer].name }}<br />
            Publié le : {{ page.date | timeFormat }}<br />
            Édité le : {{ updated | timeFormat }}
        </small></p>
        {{ content | safe }}
    </article>
    <div class="author-card">
        <img src="{{ author.mathieuhuot.img }}" alt="Image de l'auteur" />
        <p><strong>{{ author[writer].name }}</strong></p>
        <p class="author-bio">{{ author[writer].bio }}</p>
    </div>
</main>